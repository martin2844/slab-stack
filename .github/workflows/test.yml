name: Test stack contracts

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  contracts:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - name: Install validation tools
        run: sudo apt-get update && sudo apt-get install -y jq shellcheck
      - name: Shellcheck
        run: shellcheck bin/slabctl scripts/*.sh installer/*.sh installer/lib/*.sh
      - name: Validate manifest parser
        run: node --check scripts/validate-manifest.mjs
      - name: Test release contracts
        run: npm test
      - name: Validate stack contracts
        run: ./scripts/check.sh

  full-stack-smoke:
    needs: contracts
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Verify private stack end to end
        run: ./scripts/full-stack-smoke.sh
      - name: Verify versioned installer and idempotent rerun
        run: ./scripts/installer-smoke.sh
